import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.delieats.app',
  appName: 'DeliEats',
  webDir: 'dist/front/browser'
};

export default config;
